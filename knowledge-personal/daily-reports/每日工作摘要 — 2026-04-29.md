---
id: "20260429231543-2h1r"
title: "每日工作摘要 2026-04-29"
date: "2026-04-29T15:15:43+08:00"
tags: ["daily-digest","work-log"]
source: "daily-digest"
category: "documents"
summary: "2026-04-29 工作摘要"
---

# 每日工作摘要 · 2026-04-29

---

## 📊 量化交易 · 盘前选股

- 运行 quant-trader 全量扫描，生成当日盘前策略报告
- 上证/深证/创业板均处于震荡偏弱格局，整体仓位偏谨慎
- 主要障碍：代理残留导致数据接口 503，清理系统代理后恢复正常

---

## 🖥️ SCRM · 销售预警企微工作台原型

- **筛选 Tab 重构**：从「全部 / 高优先 / 已超时」改为「待处理 / 已超时 / 已处理」，三个分组数据完全独立
- **已处理卡片样式统一**：从自定义 `.done-item` 结构改为和待处理一致的 `.card` 结构
- **Mock 数据分离**：待处理和已超时卡片 `data-filter` 标记独立，避免同一卡片在两个 Tab 都出现
- **内容清理**：删除「付款停滞 / 问卷未跟进 / 异议闭环」三张卡片，保留核心预警场景

---

## ⚛️ sphere-scrm · 客户侧边栏 & CRM 后台

**分销人字段改造：**
- 分销标签改按 `kocName` 判断，不再依赖 `recommenderName`
- 新增「有效分销 / 无效分销」标签（30天窗口期判定），使用 Arco Design `<a-tag>` 展示
- 删除 CRM 后台「推荐人」整行，分销人字段扩展 `colspan="3"` 填满；将分销人移至「体验卡到期时间」同行

**侧边栏本地调试环境搭建：**
- 配置 `proxy.ts` + `HttpsProxyAgent` 解决 Vite proxy 不走系统代理导致的 ECONNREFUSED
- 调试 token/userId 改用 `.env.local` 注入（已加入 `.gitignore`，不随代码提交）
- 修复侧边栏 interceptor 污染 CRM 主应用 axios 实例的问题（同一 Tab 访问 sidebar.html 后 CRM 报 500）

---

## ☕ shengcai-web · 后端 kocName 逻辑

- 将「3个月时间窗口下限」从 CRM 详情接口的 kocName 计算链路中剥离
- 改动方式：在私有调用链加 `ignoreWindowLowerBound` 标志，`resolveKocForCrmDetail` 传 `true`，排行榜 / 批量导入路径传 `false`，互不影响
- 发现测试环境打包失败与本次改动无关：系另一分支 `feat-0428-xq` 遗漏了 `ZsxqUserSyncResultDTO` / `SyncZsxqUserParam` 两个类

---

## 🔖 技术踩坑

| 问题 | 根因 | 解法 |
|------|------|------|
| Vite proxy 连不上测试环境 | Node.js 继承系统代理 `https_proxy`，但本地 1087 端口未监听 | 翻墙工具开启；或用 `HttpsProxyAgent` 显式指定代理 |
| CRM 主应用接口 500 | sidebar.html 访问后 `sidebar_env` 写入 sessionStorage，触发 sidebar interceptor 覆盖 CRM axios 请求头 | `isSidebarContext()` 增加路径判断，非 sidebar 页面直接返回 false |
| 分销标签在侧边栏显示不全 | `.recommender-cell` 为 `inline-flex` 不换行 | 改为 `flex` + `flex-wrap: wrap` |
